[pytest]
pythonpath = .
markers =
    unit: fast unit tests (no browser, no Firestore)
    e2e: Playwright browser tests (need live server + Firestore)
    integration: live Firestore integration tests
